# Supabase Realtime credentials for multiplayer (see docs/briefs/09-multiplayer.md).
# Copy to .env for local dev. The anon key is public by design, but keep it
# out of source; RLS must be enabled on the Supabase project.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
